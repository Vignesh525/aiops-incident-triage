apiVersion: v1
kind: Service
metadata:
  name: aiops-triage-api
  namespace: aiops-triage
spec:
  selector:
    app: aiops-triage-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
